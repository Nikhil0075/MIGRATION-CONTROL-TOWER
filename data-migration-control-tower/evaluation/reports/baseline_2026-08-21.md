# Baseline Evidence Snapshot — 2026-08-21

> **Phase 1a/1b update (same day, after this baseline was captured)**: two-layer policy
> enforcement landed — capability-dispatch gate in `tools/registry.py::invoke_capability()` plus
> fail-closed tool-level `tools/policy_engine.py::authorize()`/`tools/invocation_context.py`,
> wired fully through Discovery's real code path. Found and fixed two genuine pre-existing defects
> along the way (see `docs/adr/0001-two-layer-policy-enforcement.md`'s "Real defects found"
> section): `evaluate()` crashed on `agent_key=None`, and `seed_registry.py` never actually
> persisted `permissions_key` onto any of the six core agent cards. Full suite after all fixes:
> **752 backend tests passing** (738 baseline + 14 new), **128/128 frontend**, both databases
> (`(default)` and the `mct-tests` test database) re-seeded. Billing confirmed via Cloud Console:
> ₹28,694 total trial credit, ₹254 used, expires **November 15, 2026**.

Captured before any Deploy & Harden work lands, per Phase 0. This is the honest "before" picture
— later phases' evidence reports should diff against this one rather than restate it, and no
absolute count from this file should be copied into permanent doc prose (see `docs/adr/`'s note
on why the "224/224" claim in `docs/compliance_matrix.md` went stale).

## Test suite

- Backend: **738 tests collected** (`python -m pytest --collect-only -q`), matching the prior
  independent audit's figure.
- Frontend: **128/128 tests passing**, 9 test files (`npx vitest run` under `frontend/client`).

## Cloud deployment state

Exactly one live Cloud Run service exists in `autonomous-data-migration` (us-central1):

| Service | URL | Service account | Created |
|---|---|---|---|
| `hello-agent` | `https://hello-agent-z5ab2ay6rq-uc.a.run.app` | `sa-orchestrator@autonomous-data-migration.iam.gserviceaccount.com` | 2026-08-16 |

No other Cloud Run services, no Cloud Run Jobs, no Cloud SQL instances exist yet. No
`.github/workflows/`, no Terraform state, no Artifact Registry beyond what `hello-agent`'s image
lives in.

## Policy engine coverage (before Phase 1)

`tools/policy_engine.py::evaluate()` is called directly from exactly two places:
`agents/risk/agent.py` and `agents/cutover/agent.py`. Discovery, Lineage, Planner, and Validation
do not call it. `tools/registry.py::invoke_capability()` has no policy check of any kind before
this work starts — confirmed by direct code read, not inferred from docs.

## Billing (Phase 0 checkpoint — verified via Cloud Console, 2026-08-21)

- Billing account `012089-52BECE-777B6D`, enabled and linked to the project, **denominated in
  INR**. Confirmed via Cloud Console (gcloud CLI has no command exposing this):
  - **Free Trial**, total credit **₹28,694**.
  - **₹254 used so far** (~0.9%) — **₹28,440 remaining**.
  - **Expires November 15, 2026** — later than the originally planned Oct 3 deployment-window
    end, so the Oct 3 date (not the credit) is the binding constraint for this plan's teardown
    checkpoint; there is real headroom past it if the window needs to extend.
- The originally-discussed "$300" figure was directionally correct (₹28,694 is roughly
  USD-$300-equivalent) but the account is genuinely INR — all cost estimates/budget thresholds in
  this plan should be quoted in ₹, not assumed-USD, to avoid a silent currency-conversion error.
- No Cloud Billing Budget exists yet (Phase 1d) — recommend setting alert thresholds at 50%/90%
  of ₹28,440 (remaining, not total, since ₹254 is already spent).
- Billing export (`tools/billing_export.py`) is present in code but not confirmed enabled.

## Data plane

`tools/migration_executor.py`'s only `DataPlaneExecutor` is `InMemoryExecutor`. No
`CloudRunJobExecutor` or equivalent exists yet.

## Scale harness

`evaluation/scale_harness.py --count` defaults to 250, warns outside 100–500. No 1k/5k/20k run has
been performed. `evaluation_scale_reports/current` in Firestore reflects only the most recent
250-object run, if any.

## Documentation

Missing entirely: `docs/ARCHITECTURE.md`, `docs/GOVERNANCE.md`, `docs/DURABILITY.md`,
`docs/EVALUATION.md`, `docs/DEMO.md`. `docs/RUNBOOK.md:311-312` still claims no dead-letter view
exists despite `GET /dead-letters` (`frontend/api_v1.py:2618`) already being implemented.
`docs/compliance_matrix.md:45` still claims the repo is "not under git version control" despite a
real GitHub remote (`github.com/Nikhil0075/MIGRATION-CONTROL-TOWER`) and active commit history.
